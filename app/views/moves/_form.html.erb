<%= simple_form_for [@move] do |f| %>
  <%= f.input :date %>
  <%# as: :string, %>
  <%# # input_html: { data: { controller: "datepicker" , action:"change->move-calculations#daysRented", "rental-calculations-target": "start"} } %>
  <%= f.input :start_point,
  input_html: {data: {address_autocomplete_target: "start_point"}},
  wrapper_html: {data: {controller: "address-autocomplete", address_autocomplete_api_key_value: ENV["MAPBOX_API_KEY"]}} %>
  <%= f.input :end_point %>
  <%= f.input :shipment_info %>
  <%# <p data-rental-calculations-target="days" > </p> %>
  <%# <p data-rental-calculations-target="price" > </p> %>
  <%= f.submit "Submit", :class => "btn btn-success btn-mini" %>
<% end %>
